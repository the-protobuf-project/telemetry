name: Linting

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint-go:
    name: Lint Go Code
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Check out telemetry
        uses: actions/checkout@v6
        with:
          path: telemetry

      # See ci.yaml's test-go job for why this sibling checkout exists
      # (telemetry-go/go.mod's local replace for runtime-go/telemetry).
      - name: Check out runtime-go (for the runtime-go/telemetry replace)
        uses: actions/checkout@v6
        with:
          repository: the-protobuf-project/runtime-go
          path: runtime-go

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26'

      - name: Lint Go SDK
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          working-directory: telemetry/telemetry-go
          args: --timeout=5m

      - name: Lint examples/telemetry (isolated module)
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          working-directory: telemetry/telemetry-go/examples/telemetry
          args: --timeout=5m

  lint-markdown:
    name: Lint Markdown
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Lint Markdown files
        uses: DavidAnson/markdownlint-cli2-action@v23
        with:
          globs: '**/*.md'

  lint-yaml:
    name: Lint YAML
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Lint YAML files
        uses: ibiqlik/action-yamllint@v3
        with:
          file_or_dir: .
          config_file: .yamllint.yml
          strict: true

  lint-rust:
    name: Lint Rust Code
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache Cargo
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            telemetry-rs/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Check formatting
        working-directory: ./telemetry-rs
        run: cargo fmt --all -- --check

      - name: Run Clippy
        working-directory: ./telemetry-rs
        run: cargo clippy --all-targets --all-features -- -D warnings

  lint-python:
    name: Lint Python Code
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      # Pinned: an unpinned `pip install ruff` silently upgrades on every run, so
      # an upstream release can break CI with no change to this repo. Keep this
      # in sync with the rev in .pre-commit-config.yaml.
      - name: Install linters
        run: pip install ruff==0.16.3 mypy==2.3.1

      - name: Run Ruff linter
        working-directory: ./telemetry-py
        run: ruff check .

      - name: Run Ruff formatter check
        working-directory: ./telemetry-py
        run: ruff format --check .

  check-links:
    name: Check Markdown Links
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Check markdown links
        uses: gaurav-nelson/github-action-markdown-link-check@v1
        with:
          config-file: '.markdown-link-check.json'
          folder-path: '.'
          file-extension: '.md'
          check-modified-files-only: 'no'

  check-formatting:
    name: Check Formatting
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Check for trailing whitespace
        run: |
          FILES=$(grep -rln '[[:blank:]]$' \
            --include='*.md' --include='*.py' --include='*.go' \
            --include='*.rs' --include='*.yaml' --include='*.yml' \
            --include='*.toml' --include='*.json' . 2>/dev/null \
            | grep -v '.git/' | grep -v 'node_modules/' \
            | grep -v '_deps/' | grep -v '.venv/' | head -20)
          if [ -n "$FILES" ]; then
            echo "$FILES"
            echo "::error::Trailing whitespace found"
            exit 1
          fi

      - name: Check for missing newline at end of files
        run: |
          FILES=$(find . -type f \
            \( -name '*.md' -o -name '*.py' -o -name '*.go' \
               -o -name '*.rs' -o -name '*.yaml' -o -name '*.yml' \
               -o -name '*.toml' -o -name '*.json' \) \
            ! -path './.git/*' ! -path './node_modules/*' \
            ! -path './*/_deps/*' ! -path './.venv/*' \
            -exec sh -c \
              'test -s "$1" && test "$(tail -c 1 "$1" | wc -l)" -eq 0 && echo "$1"' \
              _ {} \; | head -20)
          if [ -n "$FILES" ]; then
            echo "$FILES"
            echo "::error::Files missing newline at end"
            exit 1
          fi

      - name: Check for large files
        run: |
          FILES=$(find . -type f -size +500k \
            ! -path './.git/*' ! -path './node_modules/*' \
            ! -path './*/_deps/*' ! -path './.venv/*' \
            ! -path './*/target/*' ! -path './*/build/*' | head -20)
          if [ -n "$FILES" ]; then
            echo "$FILES"
            echo "::warning::Large files detected (>500KB)"
          fi

      - name: Check for merge conflicts
        run: |
          FILES=$(grep -rln '^<\{7\} \|^=\{7\}$\|^>\{7\} ' \
            --include='*.md' --include='*.py' --include='*.go' \
            --include='*.rs' . 2>/dev/null \
            | grep -v '.git/' | head -20)
          if [ -n "$FILES" ]; then
            echo "$FILES"
            echo "::error::Merge conflict markers found"
            exit 1
          fi
