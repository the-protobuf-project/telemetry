---
extends: default

ignore:
  - telemetry-core/deploy/production/envoy/
  - docker/envoy/

rules:
  line-length:
    max: 120
    level: warning
  document-start: disable
  truthy:
    allowed-values: ['true', 'false', 'on', 'off']
  comments-indentation: disable
