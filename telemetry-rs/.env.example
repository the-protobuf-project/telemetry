# Telemetry Environment Variables

# Enable TRACE level logs from OpenTelemetry and other dependencies
# By default, only INFO and above are shown
# ENABLE_TRACING_DEFAULT=true

# Override log level for specific modules
# RUST_LOG=info,telemetry=debug,h2=warn
